{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run and Debug: npm run debug",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run debug",
      "cwd": "${workspaceFolder}",
      "skipFiles": [
        "<node_internals>/**"
      ]
    }
  ]
}
